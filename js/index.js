/*
let currentpage = 1;

$(window).scroll(function(){
  // ef toppur á glugga er á sama stað og stærðin á skjalinu-stærðin á glugganum
  // þá er búið að scrolla alveg neðst í skjalið
  if ($(window).scrollTop() == $(document).height() - $(window).height()){
    // run our call for pagination
    currentpage += 1;
    getPopular(currentpage).then( displayMovies );
  }
});

getPopular(1).then( displayMovies );*/
